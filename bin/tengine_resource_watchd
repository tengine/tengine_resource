#!/usr/bin/env ruby
# -*- coding: utf-8 -*-
$LOAD_PATH << File.expand_path("../lib", File.dirname(__FILE__))
require "tengine_resource"

watcher = Tengine::Resource::Watcher.new(ARGV)
watcher.start

Signal.trap(:INT) { watcher.shutdown }
